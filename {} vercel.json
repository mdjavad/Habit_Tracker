{
    "builds": [{
        "src": "Habit_Tracker/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLamdasize": "15mb", "runtime": "python3.13" }
    }],
    "routes": [
        {
        "src": "/(.*)",
        "dest": "Habit_Tracker/wsgi.py"
        }
    ]
}
